version: '0'
services:
  node:
    image: node
    container_name: node
    working_dir: /project/vite-quickstart-ts
    command: bash -c "npm run dev"
    tty: true
    ports:
      - 3000:3000
    volumes:
      - /Users/jinzekun/Documents/github/vite-quickstart-ts:/project/vite-quickstart-ts
